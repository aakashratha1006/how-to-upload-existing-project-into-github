Create a new repository and add README
Copy the url of your repository 

Download Git Bash (If not downloaded).
Select the folder and select the git bash here option

Type these Commands
1. git init
2. git add . (#In order to add all files/you can add a particular file)
3. git commit -m "Added all files/or any other message which u want to give in this"
4. git status (#Just for checking the status)
5. git remote add origin  https://github.com/aakashratha1006/how-to-use-axios-template.git
(paste URL of your created repository)
6. git push origin master
7. git push origin master --force